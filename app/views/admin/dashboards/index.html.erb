<h3>ユーザー一覧</h3>
<ul>
  <%= @users.each do |user| %>
    <li>
      <% user.name %> - <%= user.email %>
      <%= link_to '削除', admin_user_path(user.id), method: :delete, date: { confirm: '本当に削除しますか？' } %>
    </li>
  <% end %>
</ul>