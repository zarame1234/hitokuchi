<% @posts.each do |post| %>
  <%= image_tag post.get_image %>
  <p><%= image_tag 'no_image.jpg' %>
  <p>店名：<%= post.shop_name %></p>
  <p>コメント:<%= post.body %></p>
  <p>ユーザーネーム<%= post.user.name %></p>
<% end %>  